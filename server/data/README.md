# Datos persistidos locales

- `users.csv` → registro de usuarios/atletas en CSV
- `app_storage.json` → resto de datos de la app (semana, rutinas, etc.) como strings por key

Estos archivos se modifican automáticamente al usar la app.
